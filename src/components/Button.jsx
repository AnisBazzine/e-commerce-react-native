import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Button({navigation, icon, title, BtnStyle,}) {
  return (
    <View>
      <Text>Button</Text>
    </View>
  )
}

const styles = StyleSheet.create({})